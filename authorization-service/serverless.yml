service: authorization-service

frameworkVersion: '3'

plugins:
  - serverless-plugin-typescript
  - serverless-dotenv-plugin

provider:
  name: aws
  runtime: nodejs16.x
  region: eu-west-1
  stage: dev
  profile: default

functions:
  basicAuthorizer:
    handler: handler.basicAuthorizer

resources:
  Outputs:
    BasicAuthorizerARN:
      Description: 'The ARN of basicAuthorizer function'
      Value: !GetAtt BasicAuthorizerLambdaFunction.Arn
